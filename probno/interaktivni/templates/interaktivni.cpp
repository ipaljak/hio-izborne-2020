#include <bits/stdc++.h>

using namespace std;

string s[3];

int main() {
  for (int i = 0; i < 3; i++) {
    cin >> s[i];
  }
  cout << "POMAK L " << endl;
  cout.flush();

  for (int i = 0; i < 3; i++) {
    cin >> s[i];
  }
  cout << "POMAK D " << endl;
  cout.flush();

  for (int i = 0; i < 3; i++) {
    cin >> s[i];
  }
  cout << "POMAK D " << endl;
  cout.flush();

  for (int i = 0; i < 3; i++) {
    cin >> s[i];
  }
  cout << "POMAK R " << endl;
  cout.flush();

  for (int i = 0; i < 3; i++) {
    cin >> s[i];
  }

  cout << "! 4" << endl;
  cout.flush();
  return 0;
}

